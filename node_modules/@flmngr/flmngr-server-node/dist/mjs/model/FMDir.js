export class FMDir {
    constructor(name, path, filled) {
        this.name = name;
        this.path = path;
        this.filled = filled;
    }
    getJSON() {
        return {
            p: (this.path.length > 0 ? "/" + this.path : "") + "/" + this.name,
            filled: this.filled,
            f: 0,
            d: 0
        };
    }
}
//# sourceMappingURL=FMDir.js.map